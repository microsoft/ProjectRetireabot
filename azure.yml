name: azure-retirebot
metadata:
  template: azure-retirebot@1.0
services:
  retirebot:
    project: .
    language: dotnet
    host: function
hooks:
  postprovision:
    shell: pwsh
    run: ./infra/scripts/post-provision.ps1
